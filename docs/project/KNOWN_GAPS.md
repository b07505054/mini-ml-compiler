# Known Gaps

Last verified: 2026-07-16.

| Gap | Status | Publication wording |
|---|---|---|
| Universal policy engine | missing | P1D.1 and E3 are evidence-driven loops; many other decisions remain declared-profile, rule-based, shadow, or experimental. |
| Triton production integration | missing | Triton is shadow/provider-shaped with real artifacts, but not production Runtime dispatch. |
| Quantization scope | advanced partial | Static INT8 is materialized and executed for the validated fused Pi operator. Full-model accuracy, graph-wide mixed precision, INT4/AWQ/GPTQ, and NPU quantization remain absent. |
| AWQ plan consistency | known inconsistency | The positive AWQ plan has global AWQ/int4 intent while per-op entries contain fp16 fallback strategy text and int4 dtype fields. Do not claim complete INT4 support. |
| Capability DB canonicality | partial | Capability profiles are intended ownership, but compiler-local profiles remain richer and synchronization is incomplete. |
| Memory hierarchy / DMA / transfer model | missing | No mature memory-space/DMA/synchronization IR or bandwidth/transfer model. |
| NPU execution | missing | NPU profiles/plans are planning-only. |
| E2 correctness | invalid | Preserve invalid verdict. |
| E2.1 compiler-only interpretation | incorrect | Reclassify as implementation-stack comparison. |
| Fake `5.7 ms` ExecuTorch number | placeholder only | Must not appear as measured evidence. |
| General superiority claims | forbidden | No “beats ExecuTorch” or universal project/runtime superiority claim. |
| AArch64 native codegen: target-specific instruction selection (generic path) | missing | The AArch64 object generated by `--variant generic` for `hir.fused_matmul_bias_relu` (`artifacts/backend_codegen/aarch64_matmul_bias_relu_vectorized/generic/`) uses only generic LLVM lowering of unmodified Linalg-derived loops. No NEON, SDOT/UDOT, or FMLA intrinsic is emitted or selected for this variant. Do not describe the generic variant as vectorized or NEON-accelerated. The separate `--variant vectorized` path (see the two rows below) does produce real NEON `fmla`, but via LLVM's own instruction selector from stock MLIR vector-dialect lowering, not a project-owned instruction-selection pattern. |
| AArch64 native codegen: instruction scheduling / register allocation | missing (project-owned); real LLVM MIR evidence now exists for a subset | Scheduling and register allocation for the generic, fully-unrolled vectorized, and tiled-vectorized (all 42 candidates) generated objects are entirely LLVM's stock `llc` behavior (greedy allocator by default; fast allocator also exercised as an analysis-only experiment). No project-owned scheduling or register-allocation pass exists for any variant. **Correction**: the assembly-derived claim of "3 real spills" for shape=8x8x8/tile=8x8x8 (previously reported here and in `register_pressure_summary.txt`) is WRONG -- real LLVM MIR analysis (`artifacts/backend_codegen/aarch64_matmul_bias_relu_mir_analysis/`) shows the true figure is 1 spill store + 1 reload load, tied to one genuine allocator spill slot (LLVM's own `type: spill-slot` + empty `callee-saved-register` annotation), not 3. This correction did not change which tile was selected for that shape. MIR-level evidence exists for 8 of 42 candidates (3 of 6 shapes: 8x8x8, 32x32x32, 64x64x64); the other 34 candidates' spill counts remain assembly-derived only, unconfirmed by MIR. Register pressure itself is reported as an explicitly labeled MIR-derived approximation (linear scan over def/kill order), not exact LLVM liveness analysis -- this LLVM 21.1.8 build is Release/Optimized with no debug-logging pressure-tracker output available. |
| AArch64 native codegen: runtime integration | missing | None of the generic, fully-unrolled vectorized, or tiled-vectorized generated AArch64 objects are loaded by the C++ runtime's `OpRegistry`, and none are a candidate in the compiler's cost model / plan selection. All run only via standalone harnesses/test tools in `mlir_passes/tools/`, executed directly on the Raspberry Pi. |
| AArch64 native codegen: generic-path performance | measured, not competitive | On the real Raspberry Pi 5, the generic (unvectorized) generated kernel is slower (median latency) than an `-O2` scalar C++ reference across all six measured shapes (8x8x8, 16x16x16, 32x32x32, 64x64x64, 32x64x32, 64x32x64) -- see `artifacts/backend_codegen/aarch64_matmul_bias_relu_tiled/benchmark_<shape>.json`. This is expected given no unrolling/vectorization is applied in this variant; do not present the generic variant as a performance improvement over existing handwritten kernels. |
| AArch64 native codegen: fully-unrolled vectorized-path scalability | resolved for practical shapes by the tiled slice | `transform.structured.vectorize_children_and_apply_patterns` with no tiling fully unrolls fixed static shapes, so object size and instruction count scale with M*N*K (32x32x32's fully-unrolled object is ~64x the instruction count of 8x8x8's, ~113KB vs. ~2.2KB; 64x64x64 was never built for this reason -- projected ~900KB). **Addressed** by `artifacts/backend_codegen/aarch64_matmul_bias_relu_tiled/` (tile+fuse+vectorize via stock Transform ops before LLVM lowering): 32x32x32 drops to 2,128 bytes (53.2x smaller) and 64x64x64 becomes practical (2,104 bytes), while measured Raspberry Pi latency is FASTER than the fully-unrolled kernel at every shape tested (median ratio 0.63-0.90x). The fully-unrolled variant is retained only as a historical/comparison baseline for the three original shapes, not as the recommended path. |
| AArch64 native codegen: tiled-path tail handling | missing (by explicit design, not oversight) | The tiled-vectorized variant's fixed 4(M)x8(N)x8(K) tile requires exact divisibility; shapes that do not divide evenly are REJECTED by `compile_hir_matmul_bias_relu_aarch64.sh --variant tiled-vectorized` with a nonzero exit and explicit error message (verified: M=N=K=10 rejected), never silently miscompiled. Use `--variant generic` or `--variant vectorized` for non-divisible shapes. General arbitrary-shape tail handling is not implemented. |
| AArch64 native codegen: tiled-path tile selection | resolved as a standalone, offline capability | Previously: the 4x8x8 tile was fixed for all shapes with no per-shape cost model. **Addressed** by `artifacts/backend_codegen/aarch64_matmul_bias_relu_tile_candidates/` -- 42 candidates (7 tiles x 6 shapes) measured on real Raspberry Pi hardware, scored by an explicit policy (`scoring_policy.json`), and selected per shape (`selected_tiles.json`). Two remaining, explicit limits: (1) selection is OFFLINE and shape-specific -- a shape outside the evaluated 6 has no selection result and needs its own candidate sweep (no dynamic-shape generalization); (2) the selector is a standalone tool, NOT integrated into `CandidateEvaluationPass` or any runtime/compiler dispatch path -- see the "Online runtime autotuning" and "General dynamic-shape tile selection" gaps below. |
| AArch64 native codegen: online runtime autotuning | missing | Tile selection (above) is entirely offline and build-time, over a fixed, pre-enumerated 42-candidate set measured once on one specific Raspberry Pi. No mechanism exists to re-measure, adapt, or select at runtime, on a different device, or for a shape outside the evaluated set. |
| AArch64 native codegen: tile-candidate selector production integration | missing (by explicit task scope, not oversight) | `tools/select_aarch64_matmul_tile_candidate.py` is a standalone, artifact-backed selector tool. It is deliberately NOT wired into `CandidateEvaluationPass` or any other production plan-selection code path in this repository. |
| AArch64 native codegen: schedule-unroll (K-loop unroll factor) production integration | missing (by explicit design, not oversight) | `tools/select_and_compile_aarch64_matmul_schedule.py` is an opt-in Python compiler-driver selector, NOT a native C++/MLIR pass and NOT wired into `CandidateEvaluationPass`, `PlanSelectionPass`, or any other production plan-selection code path. Default mode is `manual` (today's unchanged `--schedule-unroll-k` behavior); calibrated mode requires an explicit `--schedule-profile` and is never enabled automatically anywhere in this repository. Do not describe this as automatic autotuning or default schedule selection. |
| AArch64 native codegen: schedule-unroll evidence scope | measured across 6 domains, real hardware-confirmed for one CPU/dtype/kernel only | `schedule-unroll-k=4` measured fastest on Raspberry Pi 5 / Cortex-A76 / FP32 for the tiled fused matmul-bias-relu NEON microkernel across all 6 shape/tile domains tested (`artifacts/backend_codegen/aarch64_schedule_final/`). No tile larger than 8x8x8 in any dimension, no target other than this one Raspberry Pi 5, no dtype other than f32, and no kernel family other than this one microkernel have ever been measured. Do not describe `schedule-unroll-k=4` as universally optimal, and do not generalize this result to any untested target, tile, dtype, or kernel. Static backend evidence alone (spill count, code size, register pressure) correctly predicted the measured winner in only 2 of the 6 tested domains -- exact-domain Raspberry Pi calibration remains necessary for reliable winner selection, not static scoring alone. |
| AArch64 native codegen: hardware performance-counter evidence | missing | `perf` is not installed on the Raspberry Pi 5 used throughout Stages 13/16/17, and was never installed by any tool in this repository (installing new system packages on shared hardware without explicit authorization was judged out of scope). No cycle-level, IPC, branch-miss, or cache-miss hardware-counter evidence exists anywhere in the schedule-unroll evidence chain. Code size and `llvm-mca` static estimates are used only as explicitly labeled static signals, never presented as measured cache or pipeline behavior. |
| AArch64 native codegen: multi-shape single-process benchmarking | resolved (previously known reliability limit) | Previously reported: running all three shapes' correctness+benchmark loops back-to-back in one process intermittently produced incorrect output on one shape. This has since been root-caused (an un-zero-initialized `linalg.matmul` accumulator in `mlir_passes/lib/MatMulBiasReluFusionPass.cpp`, triggered by heap-address reuse across repeated/mixed calls -- not an out-of-bounds write, not scheduler- or bufferization-specific) and fixed via explicit `linalg.fill` zero-init. Verified via 1000-call repeated-call tests and 500-cycle mixed-shape/mixed-variant stress tests with allocator noise, all variants, all shapes (including the tiled slice's 6-shape, 6000-call stress run), zero failures. Cross-process isolation is still used for benchmark timing methodology (avoiding cross-call timing interference), but is no longer required for correctness. See `artifacts/backend_codegen/aarch64_matmul_bias_relu_vectorized/README.md` and `artifacts/backend_codegen/aarch64_matmul_bias_relu_tiled/README.md`. |
| AArch64 native codegen: MIR-level register-pressure model | missing (by explicit design) | `approx_peak_live_vector_registers` (`artifacts/backend_codegen/aarch64_matmul_bias_relu_mir_analysis/`) is a simple linear scan over MIR def/kill order, not a dataflow/liveness analysis -- no CFG join-point or loop back-edge handling. LLVM's own exact peak-pressure reporting (`-debug-only=regalloc` and similar) requires a debug build of LLVM; this project's LLVM 21.1.8 is Release/Optimized with debug logging compiled out, so no exact LLVM-reported pressure metric was obtainable on this toolchain. |
| AArch64 native codegen: MIR analysis coverage | partial (8 of 42 candidates, 3 of 6 shapes) | Deep MIR extraction/analysis (`artifacts/backend_codegen/aarch64_matmul_bias_relu_mir_analysis/`) covers only 8x8x8, 32x32x32, and 64x64x64 (the task's own mandated "deep-analysis" subset). 16x16x16, 32x64x32, and 64x32x64's spill/register-pressure claims remain assembly-derived only, not MIR-confirmed. |
| Pre-existing, unrelated test failures | pre-existing, not introduced by the AArch64 codegen, vectorization, tiling, tile-candidate-selection, or MIR-analysis slices | As of 2026-07-16, 20 `tools/run_mlir_pass_tests.sh` cases fail (97 total tests -- unchanged this slice, which added Python-only tooling and tests, not MLIR pipeline changes; 43 total Python unit tests across the tile-selection and MIR-analysis test suites, all passing). The dominant cause (majority of cases) is a shared `linalg.map` operand/mapper-arity verifier rejection spanning canonicalization, fusion, and quantization test fixtures alike; a smaller cluster fails on `'hir.quantize' op requires integer attribute 'clamp_min'`; a few more are FileCheck attribute-string/output drift in serving and LLM-frontend-normalization fixtures. Verified identical failing-test names before and after this slice (zero new regressions). Out of scope for the AArch64 codegen/vectorization/tiling/tile-candidate/MIR-analysis slices; needs its own fix. |
