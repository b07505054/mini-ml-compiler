Commands to regenerate this summary and verify the underlying evidence
chain. All paths relative to the ml-graph-compiler-runtime repo root.

1. Regenerate the artifact tier manifest + checksums:

   python3 /tmp/build_final_manifest.py
   (script inlined below since it lives outside the repo tree by
   convention for a one-off aggregation tool; see artifact_manifest.json
   for its exact logic reproduced in the "classify" function docstring)

2. Verify integrity of any individual artifact:

   sha256sum -c <(grep '<relative-path>' artifacts/backend_codegen/aarch64_schedule_final/checksums.txt)

3. Re-run the full per-stage evidence chain (see each stage's own
   commands.txt for exact invocations):

   Stage 12: artifacts/backend_codegen/aarch64_matmul_bias_relu_scheduling/commands.txt
   Stage 13: artifacts/backend_codegen/aarch64_matmul_bias_relu_pi_scheduling/commands.txt
   Stage 14: artifacts/backend_codegen/aarch64_matmul_bias_relu_schedule_cost_model/commands.txt
   Stage 15: artifacts/backend_codegen/aarch64_matmul_bias_relu_schedule_selection/commands.txt
   Stage 16: artifacts/backend_codegen/aarch64_matmul_bias_relu_schedule_multidomain/commands.txt
   Stage 17: artifacts/backend_codegen/aarch64_matmul_bias_relu_schedule_boundary/commands.txt

4. Full combined test suite (all 7 stages, 175 tests):

   python3 -m unittest tests.test_aarch64_mir_analysis \
     tests.test_aarch64_schedule_comparison \
     tests.test_aarch64_schedule_pi_validation \
     tests.test_aarch64_schedule_candidate_model \
     tests.test_aarch64_schedule_selection \
     tests.test_aarch64_schedule_multidomain \
     tests.test_aarch64_schedule_boundary

5. Stage 11 structural-validation regression check:

   python3 tools/validate_aarch64_tiled_schedule_structure.py \
     --input mlir_passes/test/backend_codegen/matmul_bias_relu_tiled_32x32x32.mlir \
     --tile-m 8 --tile-n 8 --tile-k 8 --schedule-unroll-k 2 \
     --output /tmp/s11_check.json
